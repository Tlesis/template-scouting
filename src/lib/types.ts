export const EVENT = {
    season: 0,
    eventCode: "event code"
};

export const fetchOptions = {
    headers: {
        "accept": "application/json"
        // TODO: auth header
    }
};

export enum AllianceColor {
    red = 0,
    blue = 1
}